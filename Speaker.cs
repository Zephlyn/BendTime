using UnityEngine;

namespace BendTime {
	/// <summary> 
	/// Basically just labels an audio source as a creature speaker.
	/// </summary>
	public class Speaker : MonoBehaviour {
		// Empty class. Just to check whether or not the audio source is a creature's speaker since it's private in BrainModuleSpeak >:(
	}
}
